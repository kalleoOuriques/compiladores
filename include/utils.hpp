#ifndef UTILS_HPP
#define UTILS_HPP

#include "token.hpp"
#include <string>

// Converte um TokenType para o nome do símbolo correspondente na gramática
std::string tokenTypeToString(TokenType type);

#endif

